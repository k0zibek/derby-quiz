{
  "name": "kahoot-horses",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm --workspace server run dev\" \"npm --workspace client run dev\"",
    "build": "npm --workspace server run build && npm --workspace client run build",
    "classroom": "npm --workspace server run classroom",
    "lint": "npm --workspace client run lint",
    "typecheck": "npm --workspace server run typecheck && npm --workspace client run typecheck",
    "test": "npm run typecheck && npm --workspace client test && npm --workspace server test",
    "audit:prod": "npm audit --omit=dev"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^25.6.0",
    "concurrently": "^9.2.1",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.1"
  }
}
