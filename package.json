{
  "name": "kahoot-horses",
  "private": true,
  "scripts": {
    "dev": "./scripts/dev.sh",
    "build": "npm --prefix client run build",
    "lint": "npm --prefix client run lint",
    "test": "npm --prefix server test"
  }
}
